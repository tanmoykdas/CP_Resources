#include<bits/stdc++.h>
using namespace std;
int n;
int solve(int i, int dis){
    if(i == n) return dis;
    dis = min()
}
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr); cout.tie(nullptr);
    cin >> n;
    int a[n];
    for(int i = 0; i < n; i++) cin >> a[i];
    int i = 0, dis = 0;
    cout << solve(i, dis);
    return 0;
}